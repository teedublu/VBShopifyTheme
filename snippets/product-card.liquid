{%- comment -%}
----------------------------------------------------------------------------------------------------------------------
MODIFIED PRODUCT CARD COMPONENT
----------------------------------------------------------------------------------------------------------------------
To allow for a product card specifc to each product type code from original copied to :
sections/product-card-default.liquid

Each product type has own product card if needed or product-card-default

{%- endcomment -%}

{%- case product.type -%}
  {%- when 'Audiobooks' -%}
    {%- render 'product-card-audiobooks', product: product, stacked: true, show_badges: true, wishlist: true -%}
  {%- when 'Audiobooks Bundle' -%}
    {%- render 'product-card-audiobooks', product: product, stacked: true, show_badges: true -%}
  {%- when 'Starter Pack' -%}
    {%- render 'product-card-starterpack', product: product, stacked: true, show_badges: true -%}
  {%- else -%}
    {%- render 'product-card-default', product: product, stacked: true, show_badges: true -%}
{%- endcase -%}